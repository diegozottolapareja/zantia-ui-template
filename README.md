
  # Make pages responsive

  This is a code bundle for Make pages responsive. The original project is available at https://www.figma.com/design/m6rLM8UsqYvRFDS4mUvL8z/Make-pages-responsive.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  